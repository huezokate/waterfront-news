node_modules/
dist/
.DS_Store
*.local

# Heavy raw source archive — not needed to run the app (web copies live in public/historic/)
JIMMY PIX/
*.xlsx

# local Figma sync dumps
figma-vars*.json

*storybook.log
storybook-static

# Storybook
storybook-static
*storybook.log
debug-storybook.log
